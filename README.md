# image-rotator
一个简单的图片旋转工具，可以将一个文件夹中的图片旋转特定的角度并且输出

使用方法：
1.找到你的图片文件位置
2.打开rotate_images.exe文件
3.输入文件夹路径（默认是这个文件所在文件夹）（记得按enter）
4.输入旋转角度（0-360）
5.输入输出文件夹路径（默认是输入文件夹的子文件夹out）（直接输入文件名字则是在输入文件夹下创立这个名字的子文件夹）
（你可以把它输出到别的地方，但是要输路径）
接下来会显示一些信息，你对照一下对不对
6.对的话输入y后按开始处理
7.结束后按enter（回车）关闭程序
